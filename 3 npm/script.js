// install a package
// npm install package name
// npm uninstall package name
// install purticular version npm i accessibility@5.1.0

const { start } = require("repl");

//undestanding node modules
//dependencies packages and dependent packages
//dev dependencies dependencies that are only used for development not used after we deployed

//what is
//npm start
//npm run dev
//we neeed to add these in scripts to execute that
// start is inbuilt so direcly npm start
// so if i use some other word like chaha need to use npm run chaha
